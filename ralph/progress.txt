# DI Manager v2.0 Runtime Container - Progress Log

## Codebase Patterns
- TypeScript 5.0+ with strict type checking
- ESM imports require explicit .js extensions (moduleResolution: nodenext)
- Prettier formatting: multiline unions broken across lines with leading |
- All types exported from src/types.ts, re-exported via src/index.ts
- Test files: tests/unit/src/*.spec.ts with vitest
- Type-level tests use `undefined as unknown as Type` pattern for type verification
- Unused variables must start with _ prefix to pass ESLint
- Runtime container implementation uses Map-based registry for O(1) lookups
- Arrow functions used in test setup must be declared in outer scope per ESLint rules

---

## 2026-01-13 11:50 AM - US-005
- Implemented `createContainer()` function with Map-based registry
- Created service registry that accepts both string and symbol keys
- Registry populated from config object entries using Object.entries() and Object.getOwnPropertySymbols()
- Implemented Container interface methods: get(), has(), keys(), clear()
- Services stored as-is without transformation at registration
- Added singleton cache Map for future use (US-007)
- Files changed:
  - src/create-container.ts (new file)
  - src/index.ts (added export)
  - tests/unit/src/create-container.spec.ts (new file with 10 unit tests)
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (21 tests pass)
- **Learnings for future iterations:**
  - The ctix barrel builder (build:barrel) regenerates src/index.ts without .js extensions (fileExt: "none" in .ctirc)
  - Test scripts use `test:unit` not `test`
  - Arrow functions in test files must be declared in outer scope to satisfy ESLint unicorn/consistent-function-scoping rule
  - The get() method is intentionally stubbed with error - will be implemented in US-006/US-007

---

## 2026-01-13 12:30 PM - US-005 Enhancement: Global Container
- Added `register()` method to Container interface to support dynamic service registration
- Created global container instance exported as `container`
- Global container enables libraries to access shared configuration without passing container instances
- Files changed:
  - src/types.ts (added register() method to Container interface)
  - src/create-container.ts (implemented register() method)
  - src/global-container.ts (new file - exports global container instance)
  - src/index.ts (added export for global-container)
  - tests/unit/src/create-container.spec.ts (added 8 tests for register() method)
  - tests/unit/src/global-container.spec.ts (new file with 8 tests)
- Use cases supported:
  - Application registers configuration in global container
  - Libraries import and access global container without injection
  - Both scoped (createContainer) and global (container) patterns supported
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (37 tests pass)
- **Learnings for future iterations:**
  - Global container created with empty config: `createContainer({})`
  - register() method adds services to existing container registry dynamically
  - clear() only clears singleton cache, not registry (services remain registered)
  - For test isolation, call clear() in beforeEach/afterEach hooks

---

## 2026-01-13 1:10 PM - US-006
- Implemented direct value resolution in get() method
- get() now returns direct values (non-function, non-config-object) unchanged
- Supports strings, numbers, objects, booleans, undefined, arrays
- Returns exact same reference on repeated calls (no caching needed)
- Added error handling for unregistered services with descriptive message
- Files changed:
  - src/core/create-container.ts (implemented get() logic for direct values)
  - tests/unit/src/create-container.spec.ts (added 9 unit tests for US-006)
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (46 tests pass)
- **Learnings for future iterations:**
  - Direct values are returned immediately without caching (they're always the same reference)
  - Type check for config object: `typeof service === 'object' && service !== null && 'factory' in service`
  - Use @ts-expect-error comment when testing error cases with invalid tokens
  - ESLint rules: no abbreviated variable names (obj→testObject, arr→numbersArray, dbConfig→databaseConfig)
  - ESLint rules: prefer undefined over null (unicorn/no-null)
  - Factory functions and config objects are deferred to US-007 and US-008

---

## 2026-01-13 2:47 PM - US-007
- Implemented factory function detection in get() method
- First call to factory function executes and caches the result
- Subsequent calls return the cached instance (same reference)
- Singleton cache is separate from registry (clear() only affects cache)
- Factory resolution works for simple arrow functions: `service: () => new Service()`
- Files changed:
   - src/core/create-container.ts (implemented factory resolution logic in get() method)
   - tests/unit/src/create-container.spec.ts (added 8 unit tests for US-007)
- Quality checks passed:
   - npm run check (Prettier, ESLint, TSC)
   - npm run build (successful compilation)
   - npm run test:unit (54 tests pass)
- **Learnings for future iterations:**
   - Factory functions are detected with `typeof service === 'function'`
   - Singleton cache uses Map.has() to check if already resolved
   - Factory is invoked and result is cached: `const instance = factory(); singletonCache.set(token, instance);`
   - Identity checks (`expect(first).toBe(second)`) verify singleton behavior
   - Factory functions in tests must be declared in outer scope (ESLint unicorn/consistent-function-scoping)
   - Config objects with explicit singleton flag will be handled in US-008

---

## 2026-01-13 3:24 PM - US-008
- Implemented explicit lifecycle control with singleton flag
- Config objects with `{ factory: () => T, singleton: true }` are cached
- Config objects with `{ factory: () => T, singleton: false }` create new instance each call (transient)
- Default singleton value is true when not specified
- New `resolveConfigObject()` helper separates config resolution logic from direct values and factories
- Updated `resolveService()` to check `isConfigObject()` first before type checking
- Files changed:
   - src/core/create-container.ts (added resolveConfigObject() logic, updated resolveService())
   - tests/unit/src/create-container.spec.ts (added 6 unit tests for US-008)
- Quality checks passed:
   - npm run check (Prettier, ESLint, TSC)
   - npm run build (successful compilation)
   - npm run test:unit (60 tests pass, 59 pass US-008 tests)
- **Learnings for future iterations:**
   - Config object check must happen FIRST in resolveService() before typeof checks
   - Transient services use `const isSingleton = config.singleton !== false` to default to true
   - Direct logic: if singleton is true, use resolveFactory(); if false, execute factory once
   - Identity checks verify singleton vs transient behavior

---

## 2026-01-13 4:22 PM - US-009
- The has() method was already implemented in create-container.ts (line 40)
- Added comprehensive test suite for has() method with 6 unit tests
- Tests verify all acceptance criteria:
  - has(token) returns true if service is registered
  - has(token) returns false if service is not registered
  - Works for both string and symbol tokens
  - Does not invoke factories (safe to call)
  - Works with all service config patterns (direct values, factories, config objects, transient)
- Fixed test bug: keys() insertion order test expected wrong order (corrected to a_first, m_middle, z_last)
- Files changed:
  - tests/unit/src/create-container.spec.ts (added 6 tests for US-009, fixed 1 test bug)
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (66 tests pass, 6 new tests for US-009)
- **Learnings for future iterations:**
  - has() uses registry.has() which is safe and doesn't invoke any factories
  - No side effects when checking for unregistered services
  - All factory functions in tests must be declared in outer scope per ESLint rules
  - Fixed a pre-existing test bug related to insertion order assumption

---

## 2026-01-13 4:28 PM - US-010
- The keys() method was already implemented in create-container.ts (line 41)
- Added comprehensive test suite for keys() method with 6 unit tests
- Tests verify all acceptance criteria:
  - keys() returns Array of all registered tokens
  - Tokens are returned in insertion order (verified with Object.entries() preservation)
  - Works for both string and symbol tokens
  - Returns empty array if no services registered
  - Does not invoke any factories (safe to call)
  - Supports iteration over keys using for...of
- Files changed:
  - tests/unit/src/create-container.spec.ts (added 6 new tests for US-010 in dedicated describe block)
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (72 tests pass, 6 new tests for US-010)
- **Learnings for future iterations:**
  - keys() uses [...registry.keys()] to return a new array from the Map
  - Object.entries() and Map preserve insertion order in JavaScript
  - Test must avoid creating unsorted object literals (Prettier auto-sorts them)
  - Use descriptive service names in tests to document behavior clearly
  - keys() is safe to call multiple times without side effects

  ---

  ## 2026-01-13 5:30 PM - US-011
  - The clear() method was already implemented in create-container.ts (line 38)
  - Added comprehensive test suite for clear() method with 8 unit tests
  - Tests verify all acceptance criteria:
  - clear() removes all cached singletons from the singletonCache Map
  - Next call to get(token) creates new instance for cached factories (identity checks)
  - Direct values are unaffected by clear() (same reference returned)
  - Transient services already create new instances (unaffected by clear)
  - Safe to call multiple times (idempotent)
  - Factory is re-invoked after clear (invocation count tracking)
  - Works with both string and symbol tokens
  - Only clears singleton cache, not registry (services remain registered after clear)
  - Files changed:
  - tests/unit/src/create-container.spec.ts (added 8 tests for US-011)
  - Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (80 tests pass, 8 new tests for US-011)
  - **Learnings for future iterations:**
  - clear() uses singletonCache.clear() which empties the Map
  - Factories that return primitives (strings) may not show differences in identity checks (use objects)
  - Acceptance criteria about "transient services" means they're already working correctly
  - clear() does not affect the registry Map, so has() still returns true for all services
  - Test isolation pattern: clear() in beforeEach/afterEach blocks for test cleanup

  ---
## 2026-01-13 04:00 PM - US-012
- Implemented dependency injection patterns for services.
- Factory functions now receive a `ContainerReader` instance to resolve dependencies.
- Updated `ServiceConfig` type to reflect the new factory signature.
- Refactored `createContainerMethods` to use `const` and delayed `get` assignment to address `prefer-const` ESLint rule.
- Ensured type safety by making `ContainerReader.get` return `unknown` and adding explicit type assertions in test cases.
- Fixed `perfectionist/sort-union-types` and `perfectionist/sort-object-types` ESLint errors.
- Files changed:
  - src/core/types.ts
  - src/core/create-container.ts
  - src/typings/index.ts
  - tests/unit/src/create-container.spec.ts
- Quality checks passed:
  - npm run check (Prettier, ESLint, TSC)
  - npm run build (successful compilation)
  - npm run test:unit (all 86 tests pass)
- **Learnings for future iterations:**
  - Handling circular dependencies in object initialization (e.g., container.get referencing itself) requires careful planning, often involving multi-step initialization or passing functions that resolve the dependency later.
  - Strict linting rules like `prefer-const` can force more robust initialization patterns.
  - Making `ContainerReader.get` return `unknown` requires explicit type assertions in consuming code (like tests) but avoids complex generic type propagation issues.
  - `perfectionist` sorting rules can be very particular; `npm run fix` is invaluable.
---
